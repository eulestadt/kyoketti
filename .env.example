# Frontend (legacy GIS / optional picker — production auth uses Worker secrets)
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
VITE_GOOGLE_API_KEY=your-api-key

# Cloudflare Worker secrets (wrangler secret put) — never commit these:
# GOOGLE_CLIENT_ID
# GOOGLE_CLIENT_SECRET
# GITHUB_CLIENT_ID
# GITHUB_CLIENT_SECRET
# SESSION_SECRET
# APP_ORIGIN=https://kyoketti.phoenix.boston
