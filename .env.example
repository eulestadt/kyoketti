# Create a Google Cloud project, enable Google Drive API + Google Picker API,
# then create an OAuth 2.0 Web Client ID. Add your app origin to authorized JS origins.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# Optional: API key used by the Drive folder picker
VITE_GOOGLE_API_KEY=your-api-key
